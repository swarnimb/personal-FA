# AmIBroke Finance Tracker

Self-hosted personal finance dashboard that syncs bank accounts, investments, and crypto
into one dark-mode desktop app. Runs locally — all data stays on your machine.

## Setup

```bash
cp .env.example .env
# Fill in real values in .env
```

[Add install and run commands after stack is confirmed in @plan.]

## Environment Variables

See `.env.example` for required variables.

## Tests

[Add test command after stack is confirmed in @plan.]
